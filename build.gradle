allprojects {
    group = 'org.kromo.lambdabus'
    version = '0.12.0-SNAPSHOT'

    repositories { mavenCentral() }
}

subprojects {
    apply plugin: 'java'

    if (plugins.hasPlugin('biz.aQute.bnd')) {
        repositories { mavenCentral() }
    }
}
